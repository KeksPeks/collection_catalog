# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs. The analyzer can also be invoked from the command line by running
# `flutter analyze`.

include: package:flutter_lints/flutter.yaml

# Информационные lint-диагностики не должны блокировать CI.
# Ошибки и предупреждения по-прежнему являются ошибками проверки.
analyzer:
  errors:
    curly_braces_in_flow_control_structures: ignore
    unnecessary_underscores: ignore
    use_build_context_synchronously: ignore
    prefer_null_aware_operators: ignore
    avoid_relative_lib_imports: ignore

linter:
  rules:
    # avoid_print: false
    # prefer_single_quotes: true

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
