# Рабочие подсказки для продолжения

## 2026-08-26

### Выполнено
- Исправлен экран «Мои коллекции».
- При выборе загруженного каталога теперь открывается непосредственно список его предметов (`ItemsPage`), без промежуточной технической страницы `CollectionDetailPage`.
- Убрано лишнее описание «Каталог сохранён на устройстве» из карточки коллекции.
- Название загруженной коллекции синхронизируется с названием направления каталога по `templateId`.
- Для `coins` используется направление `numismatics`, поэтому «Нумизматика» больше не должна отображаться как «Монеты».
- Поиск в «Мои коллекции» работает по отображаемому синхронизированному названию.
- При открытии `ItemsPage` передаётся синхронизированное имя коллекции.
- Для многоколоночного режима сохранена компактная карточка с защитой от переполнения текста.

### Подтвержденная рабочая точка до изменений
- `f6eef46` — состояние, которое ранее было подтверждено пользователем как рабочее.

### Важно
- Рабочая ветка проекта: `develop`.
- `main` не изменять до отдельного подтверждения после проверки `develop`.
- После изменений локальную копию необходимо синхронизировать с `origin/develop`, затем выполнить `flutter clean`, `flutter pub get`, `flutter analyze` и `flutter run`.

### Предстоит проверить
- Фактическое отображение «Нумизматика» в «Мои коллекции» после загрузки каталога.
- Переход из «Мои коллекции» сразу в предметы выбранного каталога.
- Отсутствие промежуточной непонятной страницы.
- Отображение названий для остальных каталогов и локализаций.
- Корректность многоколоночного режима без `Bottom overflowed`.

## 2026-09-01

### MTG / Scryfall parser
- Добавлен `tools/mtg/parser.py` в ветку `develop`.
- Версия парсера: `1.3.0`.
- Парсер работает с серверным хранилищем `G:\CollectionServer\collections\mtg\`.
- Режим `--images-only` читает существующий `database/cards.jsonl`, не скачивает Bulk Data и не пересоздаёт `cards.jsonl`.
- Поддерживаются параллельная загрузка изображений, пропуск уже корректных JPEG, временные `.part` файлы и атомарное создание `image_index.jsonl`.
- Коммит добавления: `787e041febc99c2aa6646e24cdd64abd26453933`.
- Локальная проверка через контейнер невозможна из-за отсутствия сетевого доступа к GitHub; файл после записи повторно проверен чтением из ветки `develop`.
- `main` не изменять до отдельной успешной проверки `develop`.

### Исправление подключения приложения к серверу
- Проверено: FastAPI уже работает на `0.0.0.0:8000`, локальный запрос `http://127.0.0.1:8000/api/health` возвращает `{"status":"ok"}`.
- Компьютер в LAN имеет IPv4 `192.168.0.7`.
- Приложение ранее использовало `http://127.0.0.1:8000` как адрес API по умолчанию. Для Android это адрес телефона, а не компьютера.
- Изменён `lib/core/network/api_client.dart`: адрес API по умолчанию теперь `http://192.168.0.7:8000`.
- `COLLECTION_API_BASE_URL` по-прежнему может переопределить адрес через `--dart-define`.
- Коммит исправления: `fb46aa13e64b7e5d0d27cf4d92512d30648c8080`.
- Следующий этап: синхронизировать `develop`, затем выполнить проверку №1 через `flutter analyze` и `flutter run`; после исправлений провести вторую полную проверку и только после успешного результата переносить в `main`.
